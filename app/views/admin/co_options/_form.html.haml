= form_with model: co_option, url: co_option.persisted? ? [:admin, co_option] : [:admin, :co_options], class: 'admin-form' do |f|
  = render partial: 'admin/shared/errors', locals: {errors: f.object.errors}
  .admin-form_field
    = f.label :occurred_on
    .input
      = f.text_field :occurred_on
  .admin-form_field
    = f.label :meeting_type
    .input
      = f.select :meeting_type, %w(monthly annual special budget finance)

  .admin-form_actions
    = f.submit 'Save', class: 'button'